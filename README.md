## Project Structure

The `main` branch contains three key components:
- a scrollytelling article,
- an interactive dashboard, and
- a technical paper.

The `analysis` branch contains all datasets, analysis scripts, and derived outputs used in developing the scrollytelling article. 